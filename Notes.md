### Dependecies:
- "bcryptjs" helps with password encrypting
- "cors" decides what websites can access our backend
- "dotenv" for .env
- "express" backend framework
- "mongoose"
- "morgan"
- "joi" it validates the data el gayaly

### Folders:
- "config" is for database

**I can't type that fast**

### Requests:
- "GET" to retrive items from db
- "POST" for data creation
- "PUT" updates the entire record
- "PATCH" partially updates the record
- "DELETE" just deletes
- "QUERY" same as GET but accepts a body